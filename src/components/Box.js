import styled from '@emotion/styled'
import {
  alignContent,
  alignItems,
  alignSelf,
  background,
  border,
  borderBottom,
  borderLeft,
  borderRight,
  borderTop,
  color,
  display,
  flex,
  flexBasis,
  flexDirection,
  flexWrap,
  height,
  justifyContent,
  justifySelf,
  maxWidth,
  minHeight,
  order,
  overflow,
  position,
  space,
  width,
  zIndex
} from 'styled-system'

const Box = styled('div')`
${alignContent}
${alignItems}
${alignSelf}
${background}
${border}
${borderBottom}
${borderLeft}
${borderRight}
${borderTop}
${color}
${overflow}
${display}
${flex}
${flexBasis}
${flexDirection}
${flexWrap}
${height}
${justifyContent}
${justifySelf}
${maxWidth}
${minHeight}
${order}
${position}
${space}
${width}
${zIndex}
`

export default Box
